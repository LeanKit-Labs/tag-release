import test from "ava";
import utils from "../../src/utils";
import sinon from "sinon";

test.beforeEach( t => {
} );

test.afterEach( t => {
} );

// isPublishable = true

// if there

// there is no private key and no publishConfig.registry
// there is no private key, but an empty publishConfig.registry ("")
// there is no private key, but a value publishConfig.registry

// isPublishable = false

// this is a private key set to true and no

test( "isPublishable", t => {
	t.ok( true );
} );
